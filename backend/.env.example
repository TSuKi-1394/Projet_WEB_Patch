# Configuration de l'environnement - TEMPLATE
# ⚠️ NE PAS COMMITER LE FICHIER .env AVEC DES VRAIES VALEURS
# Copier ce fichier en .env et remplacer les valeurs

# Port du serveur
PORT=8000

# Environnement (development, production)
NODE_ENV=development

# Configuration de la base de données PostgreSQL
# ⚠️ CHANGER CES VALEURS EN PRODUCTION
DB_HOST=database
DB_PORT=5432
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION

# Clé secrète pour les sessions
# ⚠️ GÉNÉRER UNE CLÉ ALÉATOIRE FORTE EN PRODUCTION
# Exemple: openssl rand -base64 32
SECRET_KEY=CHANGE_THIS_SECRET_KEY_IN_PRODUCTION

# Configuration CORS - Origines autorisées
CORS_ORIGIN=http://localhost:3000

# Limite de requêtes par fenêtre de temps (rate limiting)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
